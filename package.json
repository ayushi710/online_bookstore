{
  "name": "login_server",
  "version": "1.0.0",
  "description": "check user entered information with database and allow user to login the page",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "mongodb": "^3.0.5",
    "mongoose": "^5.0.11",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^1.17.2"
  }
}
